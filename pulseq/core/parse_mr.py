import numpy as np

from pulseq.core.holder import Holder
from pulseq.core.Sequence.sequence import Sequence


def parse_mr(*seq_files):
    """

    Parameters
    ----------

    Returns
    -------
    """
    seq = Sequence()
    for f in seq_files:
        pass
